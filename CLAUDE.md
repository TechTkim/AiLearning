# AiLearning — 중학교 2학년 기술·가정 AI 수업용 웹앱

이 저장소는 GitHub Pages(https://techtkim.github.io/AiLearning/)로 배포되는 수업용 웹앱 모음이다.
학생은 학교 iPad(Safari, iPadOS 18)에서 이 주소로 앱을 연다. 교사는 이 노트북의 VS Code + Live Server로 미리 보며 수정한다.

## 파일
- `snake_v3.html` — 보상 설계 실험실(스네이크 AI, 현재 수업용): 강화학습 실습 Ⅰ·Ⅱ (Q-학습, 뽑기 게임, 사람 vs AI 대결, 함정, 독사과, 술래잡기)
- `snake_v2.html` — 이전 버전(더 이상 배포하지 않음, 참고용으로만 보관)
- `snake_v1.html` — 이전 버전(더 이상 배포하지 않음, 참고용으로만 보관)
- `spam_v1.html` — 우리 반 스팸 필터 AI(정보 vs 광고): 지도학습(나이브 베이즈) + 취향 지도(k-평균) + AI 속이기
- (추가 예정) `game_v1.html` 내가 AI다 게임 진행판, `draw_v1.html` AI 학습 그림판

## 절대 지킬 규칙
1. **단일 HTML 파일.** CSS·JS를 파일 안에 그대로 둔다. 파일을 나누지 않는다.
2. **외부 의존 0.** CDN·라이브러리·폰트 링크·외부 API·localStorage를 새로 추가하지 않는다. 학교 네트워크 필터 때문이다.
3. **iPad Safari 기준.** 터치 조작(pointer 이벤트), 가로 1024px 안팎 화면. 키보드 없이도 모든 기능이 되어야 한다.
4. **학습 결과는 결정적이어야 한다.** `mkRng(seed)` 고정 시드, 평가 시 난수 분리(`evalScore`의 rng 저장/복원)를 건드리지 않는다. Q-학습·나이브 베이즈 로직을 수정해야 한다면 반드시 이유를 먼저 설명하고 확인을 받는다.
5. **문구·숫자 수정이 기본 작업이다.** 요청이 "안내문/색/크기/기본값" 수정이면 자바스크립트 로직은 손대지 않는다.
6. **버전 규칙.** 학생에게 배포된 파일(v1)을 크게 바꿀 때는 `snake_v2.html`처럼 새 파일로 만든다(iPad 캐시·수업 중 변경 방지). 사소한 문구 수정은 같은 파일에 해도 된다.
7. 한국어 UI. 학생 대상 문장은 짧고 구체적으로. 전각 번호(①②)는 활동지·슬라이드와 맞추기 위해 유지.

## 디자인 시스템
- 색: Navy `#1B3A6B`(제목·기본), Teal `#006D77`(정보·지도학습·1차), Orange `#D05A2A`(강조·광고·강화학습), Green `#2E7D32`(정리·성공), Red `#C43C2A`(경고·함정)
- 각 파일 상단 `:root` CSS 변수(`--navy`, `--teal`, `--orange`, `--green`, `--red`)를 통해서만 색을 바꾼다.
- 글꼴은 시스템 폰트(-apple-system, Apple SD Gothic Neo, Malgun Gothic). 웹폰트 링크 추가 금지(규칙 2).
- 버튼 최소 높이 44px(터치). 텍스트 15px 이상.

## 작업 방식
- 수정 전에 어느 줄을 바꿀지 짧게 말하고, 수정 후에는 브라우저에서 확인할 항목을 한 줄로 알려준다.
- 수정이 끝나면 요청 시 `git add -A && git commit -m "..." && git push`까지 수행한다. 커밋 메시지는 한국어 한 줄.
- 파일이 길어도 관련 부분만 편집한다. 파일 전체를 다시 쓰지 않는다.

## 수업 맥락(참고)
- 3차시: AI 판별 게임·영상으로 AI의 학습 방식 이해 → 4차시(2회): 스네이크 실습 Ⅰ "보상으로 가르친다", 실습 Ⅱ "AI를 고장 내고 고친다"
- 핵심 개념: AI는 예시·경험(보상)으로 배운다 / 데이터·보상이 잘못되면 자신 있게 틀린다 / AI를 고치는 일 = 데이터·보상·입력(눈)을 고치는 일
